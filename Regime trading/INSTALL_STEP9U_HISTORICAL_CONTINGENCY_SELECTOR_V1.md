# Installation

Use the supplied Windows installer from the project root. It verifies the package, frozen Step 9T artifacts, protected-file hashes, dedicated tests, deterministic verifier, replay outputs, and the full project suite.

The installer must not be used if Step 9U files already exist. Do not overwrite or merge a prior Step 9U version.
